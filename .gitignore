node_modules/
dist/
*.log
.DS_Store
.claude/

# 用户私密配置（含 API Key）——不进仓库
config.json

# TTS 磁盘缓存
renderer/.tts-cache/

# Live2D 模型：授权 §4.1.1 禁止再分发，不进仓库、不进公开 Release。
# 本地自用可保留在磁盘（个人/小规模使用是允许的）。见 NOTICE。
renderer/models/
